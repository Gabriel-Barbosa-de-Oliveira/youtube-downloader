import { expect, describe, test, jest, beforeEach } from "@jest/globals"
import { Server } from "http"

const originalHttp = jest.createMockFromModule("http")

describe("Http Download Test", () => {

    test.todo("should receive url from input and check if it is valid")
    test.todo("should download a file from a URL")

})